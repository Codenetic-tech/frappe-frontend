version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    environment:
      - BACKEND_URL=http://192.168.1.203
      - BACKEND_HOST=pulse.gopocket.in
    restart: always
